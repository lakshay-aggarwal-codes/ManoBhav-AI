# Local development — point this at your FastAPI backend
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production example (set this in Vercel's Environment Variables UI instead
# of committing it — this file is just a template)
# NEXT_PUBLIC_API_URL=https://manobhav-ai-backend.onrender.com
